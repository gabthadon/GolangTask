HOW TO RUN PROGRAM
1. Make sure you have Go installed 
2. Open a terminal or command prompt.
3. Navigate to the directory where concurrent_log_processor.go is saved.
4. Run the following command:  'go run concurrent_log_processor.go'
